# AI 漫剧工厂 · 夜间生产与早晨验收手册

## 目标

晚上只提交已经完成人工创作审批和资产审批的单集或整季；后台按单 GPU 串行生成，成功镜头不重复运行，失败镜头可续跑。早晨先看任务、预览和质检，再导出上传包。任何模型、平台或素材的商用范围须由运营方独立确认。

## 当前 RTX 3090 稳定基线

- MiniMax H3 Turbo LoRA：`minimax_h3_turbo_v4_step600_ema.safetensors`
- Scheduler / steps / strength：`simple / 8 / 1.0`
- AV sampling：ComfyUI 原生 `ModelSamplingAV`；24 fps；视频 shift 12、音频 shift 3
- Attention：SageAttention 2（RTX 3090 / Ampere）；不要叠加未适配 H3 的 TeaCache/EasyCache
- 单 GPU 视频并发：1；不要把整集封装成单个 Comfy 节点
- 默认平台母版：竖屏9:16为720×1280、横屏16:9为1280×720、30 fps、H.264 High/yuv420p、AAC 48 kHz双声道、-14 LUFS、faststart

## 晚上操作

1. 在“创作简报”输入主题、故事梗概、风格、集数和每集时长；MiniMax 生成故事、人物、场景、分镜、对白和字幕合同。
2. 逐项审核并批准故事、人物、分镜。人物或故事不一致时只重生目标项，不要批准后再改圣经。
3. 保存合同，生成人物与场景资产。逐人物、逐场景看原图；未通过就“拒绝并重生”。
4. 资产门禁显示全部批准后启动单集或整季。H3 动态模式不得并发多个视频任务。
5. 任务失败后先看错误和拒收审计；只点一次“继续未完成任务”，已成功镜头会跳过。
6. 多人物动态 H3 若发生人数变化、切单人、人物漂移或随机文字，正式拒收。不要无限重抽；最多两次定向重生，之后进入 dead letter 或人工拆镜。禁止用一张群像锚生成整集“连续性安全模式”并冒充剧情镜头。
7. 夜间不要更新 ComfyUI core、自定义节点、LoRA、Torch、CUDA 或显卡驱动。

## 早晨验收

1. 页面应显示任务总数、成功/运行/失败和整体进度；逐镜能播放当前有效 MP4，历史拒收片只在审计区。
2. 每镜检查首/中/尾：人物数量与身份、服装、场景、构图、画面随机文字、前后镜连接。
3. 对白时检查声音和后期字幕；连续性安全镜头的源 MP4 应无字幕，SRT/VTT/ASS 存在，最终交付才烧录一次。
4. 全部镜头成功后选择平台规格并导出。页面应提供最终 MP4 预览、MP4 下载、delivery ZIP 下载及 manifest 摘要。
5. manifest 必须满足：`subtitles.burn_stage=final_delivery`、`legacy_preburned_job_ids=[]`、视频/音频探针符合目标规格。
6. 随机抽听整片并完整播放一次，再人工上传。不得把“技术探针通过”等同于内容可以发布。

## 故障处置

- ComfyUI 断开/超时：等待服务恢复后点一次继续；禁止重复点击多个 worker。
- 同一镜失败：最多两次定向重生；再失败进入 dead letter 或人工拆镜。静态锚仅允许作为单镜审计样片，不能进入发布包。
- 机器/页面重启：输入原项目 ID 恢复；不要新建同名项目或手动复制旧 MP4 覆盖。
- 磁盘不足：停止新任务，保留 `episode.json`、SQLite、graph/cues/manifest 和 approved assets；先归档 rejected 历史。
- 质量不确定：保持 failed/待审核，不进入合片。

## 商业上线前台账

每个模型、LoRA、字体、BGM、音效、参考图和第三方代码记录：来源、版本、哈希、许可证、商用条件、署名要求和地域限制。代码为 MIT 不代表模型权重、素材或生成结果自动拥有可商用权利。

## 已知边界

MiniMax H3 是生成式音视频模型，提示词不能保证逐字口型和精确动作。RTX 3090 上的严格五人长镜头已经出现模型服从上限；生产方案必须把多人事件拆成群像建立、单/双人动作、反应和道具特写。低动态群像锚只能用于单镜审计，自动内容 QA 未通过时禁止导出。

`ep_1786340037` 的六段视频虽然容器和音轨合格，但 decoded video 完全相同，已被撤销发布资格并作为负样本保留。今后每个镜头必须依次通过技术生成、自动内容 QA、人工逐镜批准和整集发布批准；缺任一层都不能生成可发布下载包。详细标准见 `SHORT_DRAMA_PRODUCTION_STANDARD.md`。
