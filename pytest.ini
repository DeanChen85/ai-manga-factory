[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra

# Root-level test_*.py files are legacy manual smoke scripts that may contact
# local services during import.  Automated collection is intentionally limited
# to the hermetic tests/ suite; manual scripts remain runnable explicitly.
